rustdesk.hungbm.com

oF3ilMqnsAfHRr6vNZVec1WDwSyUFpHejkBmBLpA3tg=

passwd

1

12345678a@

sudo steamos-chroot --disk /dev/nvme0n1 --partset A --no-overlay

sudo ~/tools/repair_device.sh chroot
rm -f /var/lib/overlays/etc/upper/passwd
rm -f /var/lib/overlays/etc/upper/shadow

sửa file /etc/systemd/resolved.conf

DNS=1.1.1.1#cloudflare-dns.com 1.0.0.1#cloudflare-dns.com 2606:4700:4700::1111#cloudflare-dns.com 2606:4700:4700::1001#cloudflare-dns.com
DNSOverTLS=yes

sudo systemctl restart systemd-resolved

---------------
flatpak install flathub-beta net.lutris.Lutris
---------------
sudo flatpak repair

flatpak update --user --appstream

flatpak remotes (display the flatpak repos)

sudo flatpak remote-delete flathub (this will delete the repo)

sudo flatpak remote-delete flathub-beta (this will delete the repo)

flatpak remotes (should be blank now)

sudo flatpak remote-add flathub https://flathub.org/repo/flathub.flatpakrepo (this wll re-add the repo)

sudo flatpak remote-add flathub-beta https://flathub.org/beta-repo/flathub-beta.flatpakrepo (this wll re-add the repo)

flatpak remotes (this will now show the 2 added repos)
